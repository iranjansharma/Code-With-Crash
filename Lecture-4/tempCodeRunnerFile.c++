 // int a = 4;
    // int b = 6;

    // cout << "a & b = " << (a & b) << endl;
    // cout << "a or b = " << (a | b) << endl;
    // cout << "b Not = " << ~b << endl;
    // cout << "a Not = " << ~a << endl;
    // cout << "a ^ b = " << (a ^ b) << endl;

    // cout << (17 >> 1) << endl;
    // cout << (17 >> 2) << endl;
    // cout << (21 << 1) << endl;
    // cout << (21 << 2) << endl;